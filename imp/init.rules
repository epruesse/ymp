from imp.config import parse_mapfiles

parse_mapfiles()

PAIRS   = config['pairnames']
RNA     = config['rrna']['genes']

wildcard_constraints:
    dir    = "[^/]+",
    dir1   = "[^/]+",
    dir2   = "[^/]+",
    sample = "[^/]+",
    fracid = "[0-9][0-9]",
    coverage = "[0-9]+"
